# Moved

The standalone Hono API under this folder is **retired**.

Checkout + webhooks now live in the Next.js app at repo root:

- `/api/buy/:app`
- `/api/webhooks/lemon`
- `/api/test` (mock)

See [`../README.md`](../README.md) and [`../LEMON.md`](../LEMON.md).
